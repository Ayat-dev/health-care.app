# ClinicApp — Backlog d'amélioration & durcissement (le tracker transverse)

> **But de ce fichier** : suivre, pas à pas et **entre sessions**, tout ce qui sépare le projet
> d'un EHR déployable en production réelle. Issu de l'analyse comparative du 2026-06-19
> (meilleurs systèmes du marché, sécurité HIPAA/OWASP, tendances UI/UX 2025-2026) **croisée
> avec un audit du code réel**.
>
> **Comment l'utiliser** :
> - `[ ]` todo · `[~]` en cours · `[x]` fait. Coche au fur et à mesure, comme le ledger de `CLAUDE.md`.
> - Un item par session de préférence ; `mvnd` pour vérifier ; mets à jour la case + une note de résultat.
> - Ordre conseillé : **P1 d'abord** (bloquants prod), puis P2, P3, P4.
> - Ce fichier est référencé depuis `CLAUDE.md` (« NEXT UP »). Le ledger de `CLAUDE.md` reste
>   la source de vérité pour les **modules fonctionnels** ; **ce fichier** est la source de
>   vérité pour le **non-fonctionnel + différenciation**.
>
> **État de référence au 2026-06-19** : 14 modules cliniques faits, RBAC propre (corrigé ce
> jour : variable `th:each` réservée `mod`→`navMod` qui 500-ait toutes les pages ; gating
> lecture pharmacie). Socle fonctionnel solide ; les manques sont surtout **non-fonctionnels**.

---

## 🔴 P1 — Bloquants production (sécurité & fiabilité)

### P1.1 — Séparation des profils Spring dev / prod
- [x] Statut : **fait** (2026-06-19)
- **Réalisé** : `application.properties` réduit à une base neutre (`spring.profiles.active=dev`, Flyway, OSIV off, multipart, upload-dir, expiration JWT) ; `application-dev.properties` (H2, console H2 on, secret de dev) ; `application-prod.properties` (PostgreSQL + secrets via env, **sans défaut → fail-fast**, console H2 off). `DataInitializer` gated `@Profile("!prod")` (plus de seed démo ni de `admin/admin123` en prod) ; nouveau `ProdDataInitializer` (`@Profile("prod")`) crée l'admin depuis `CLINIC_ADMIN_USERNAME/PASSWORD` si base vide. `docker-compose.yml` : `SPRING_PROFILES_ACTIVE=prod`, healthcheck Postgres + `depends_on: condition: service_healthy`, volume `backend_uploads`. `.env.example` (racine) créé ; `.env` déjà gitignoré.
- **Vérifié** : dev (défaut) démarre, Flyway v13, login admin/admin123 + dashboard 200 ; prod **sans secrets → refuse de démarrer** (`Could not resolve placeholder 'JWT_SECRET'`, rien sur 8080) ; prod avec secrets (testé sur H2, même chemin de code, Docker indispo pour Postgres) → admin bootstrappé depuis l'env (login OK, dashboard 200), `admin/admin123` rejeté (`/login?error=true`), `/patients` 200 sans donnée démo, **h2-console 404** (vs 302 en dev).
- **Reste à confirmer au déploiement réel** : boot complet sur PostgreSQL (non testable ici, Docker absent) — la config + le driver au pom sont en place.
- **Pourquoi** : aujourd'hui un **seul** `application.properties` (H2 en mémoire, console H2 activée,
  `show-sql`, secret JWT par défaut en dur). Un déploiement « prod » tournerait sur la config de dev.
- **Où** : `backend/src/main/resources/application.properties` (existant, dev).
- **Étapes** :
  1. Renommer la config H2 actuelle en `application-dev.properties` ; garder un `application.properties` minimal neutre (`spring.profiles.active=dev` par défaut).
  2. Créer `application-prod.properties` : datasource PostgreSQL, `spring.h2.console.enabled=false`, `spring.jpa.show-sql=false`, `ddl-auto=validate`.
  3. Externaliser **tous** les secrets en variables d'env : `app.jwt.secret`, `spring.datasource.password`, etc. (`${DB_PASSWORD}`, `${JWT_SECRET}`).
  4. `docker-compose.yml` + `.env.example` : injecter ces variables ; `SPRING_PROFILES_ACTIVE=prod`.
- **Critère d'acceptation** : `SPRING_PROFILES_ACTIVE=prod` démarre sur PostgreSQL, console H2 inaccessible, app refuse de démarrer si `JWT_SECRET` absent (fail-fast).

### P1.2 — Journal d'audit (qui / quoi / quand)
- [x] Statut : **fait** (2026-06-19)
- **Réalisé** : nouveau pkg `com.clinic.backend.audit` — `AuditLog` (entité append-only), `AuditLogRepository` (search filtré + `distinctEntityTypes/Actions`), `AuditService` (`record` en `@Transactional(REQUIRES_NEW)`, résout auteur via `SecurityContextHolder` + IP/User-Agent via `RequestContextHolder`, **n'échoue jamais vers l'appelant**), annotation `@Audited(action, entity)` + `AuditAspect` (`@AfterReturning`, entityId déduit du `getId()` du retour sinon 1er arg `Long`). `V14__audit_log_table.sql`. Vue admin lecture seule `/admin/audit` (`AdminAuditWebController`, `@PreAuthorize("hasRole('ADMIN')")`, filtres utilisateur/entité/action/dates, cap 300) + `admin/audit/list.html`. Nav : entrée `ADMIN_AUDIT` (📜) dans `Module` (auto-visible ADMIN via `EnumSet.allOf`). 14 méthodes annotées : Patient create/update, Lab create/validate/cancel, Invoice create/payment/cancel, Consultation create/complete, Dispensation dispense, Hospitalization admit/discharge/transfer.
- **Vérifié** : Flyway→v14, build OK, app démarre ; create patient + record payment + cancel lab → **3 lignes d'audit** avec auteur réel (`admin`), entityId (Patient #4, Invoice #1, LabRequest #2) et IP (`::1`) ; les actions métier réussissent (302) → la trace en tx séparée ne casse rien ; filtres entité/action OK ; non-admin (laborantin) → **403**.
- **Pourquoi** : manipulation de données médicales → exigence légale (HIPAA-like). **Aucun** audit aujourd'hui. Déjà listé « différé » au module 15.
- **Où** : nouveau pkg `com.clinic.backend.audit`.
- **Étapes** :
  1. Table `audit_log` (V14) : `id, user_id, username, action, entity_type, entity_id, ip_address, user_agent, details, created_at`.
  2. Capture via AOP `@Aspect` autour des `service` (create/update/delete/transitions de statut) **ou** `@EntityListener` JPA (`@PostPersist/@PostUpdate/@PostRemove`).
  3. Vue admin `/admin/audit` (lecture seule, filtres user/entité/date), `@PreAuthorize("hasRole('ADMIN')")`.
- **Critère d'acceptation** : créer/modifier un patient, valider un labo, encaisser une facture → 1 ligne `audit_log` chacune avec l'utilisateur réel.

### P1.3 — Anti-brute-force (verrouillage + rate-limit login)
- [x] Statut : **fait** (2026-06-19)
- **Réalisé** : `V15__user_login_lockout.sql` ajoute `failed_attempts INT NOT NULL DEFAULT 0` + `locked_until TIMESTAMP` sur `users` ; `User` porte les champs et `isAccountNonLocked()` = `lockedUntil == null || lockedUntil < now()` (auto-déverrouillage à expiration). `LoginAttemptService` (`@Transactional`) : `loginFailed` incrémente (un verrou expiré repart de 0), verrouille 15 min à `MAX_ATTEMPTS=5` ; `loginSucceeded` remet compteur+verrou à zéro. Branché **par événements** Spring Security (`AuthenticationEventListener` : `AuthenticationSuccessEvent`/`AuthenticationFailureBadCredentialsEvent`) → couvre web **et** API d'un coup ; `LockedException` ignorée (sinon le verrou se prolongerait). `DefaultAuthenticationEventPublisher` exposé en bean (sinon publisher no-op). `LoginFailureHandler` (web) route `LockedException`→`/login?locked=true`, reste→`/login?error=true` ; message « Compte verrouillé… 15 minutes » dans `login.html`. `AuthController.login` (API) catch → **423 Locked** / **401** JSON propre (au lieu d'un 500 brut). On n'écoute pas les `BadCredentials` du `JwtFilter` (il pose l'auth en direct, pas d'événement → pas de bruit par requête).
- **Vérifié** (H2, app bootée Flyway→v15) : **API** 5 mauvais mdp sur `admin` → 5×401 JSON, puis bon mdp → **423 Locked** (verrou actif) ; `dr.martin` intact → 200. **Reset prouvé** : 4 échecs + 1 succès ×2 (8 échecs au total) → jamais verrouillé, dernier succès 200. **Web** (avec token CSRF + session) : `admin` verrouillé → **302 → `/login?locked=true`**.
- **Pourquoi** : aucun lockout ni rate-limit ; `/login` et `/api/auth/login` exposés au bourrinage.
- **Où** : `User` (a déjà `active`), `SecurityConfig`, `service` auth.
- **Étapes** :
  1. Colonnes `failed_attempts INT`, `locked_until TIMESTAMPTZ` sur `users` (V15).
  2. `AuthenticationFailureHandler` : incrémente ; à N (ex. 5) → `locked_until = now()+15min`. Succès → reset.
  3. `isAccountNonLocked()` reflète `locked_until`.
  4. (Option) rate-limit IP via Bucket4j sur les endpoints de login. — **non fait** (lockout par compte suffit pour le critère ; rate-limit IP reportable si besoin DoS distribué).
- **Critère d'acceptation** : 5 mauvais mots de passe → compte verrouillé 15 min, message clair. — ✅

### P1.4 — `@RestControllerAdvice` global (fin des 500 bruts)
- [x] Statut : **fait** (déjà livré par le commit `e2db645` du 2026-06-14, *avant* la rédaction de ce backlog — la prémisse « il manque le pendant API » était fausse). Vérifié 2026-06-19.
- **Constat** : `config/GlobalExceptionHandler.java` est un `@RestControllerAdvice(basePackages = "com.clinic.backend.controller.api")` — il couvre donc **les 16 contrôleurs API** (tous dans `controller.api`). Mapping en place : `MethodArgumentNotValidException`→400, `IllegalArgumentException`→400, `IllegalStateException`→**409**, `AuthenticationException`→401, `AccessDeniedException`→403, `ResourceNotFoundException`→404, fallback `Exception`→500, au format standard `{timestamp,status,error,message,path}`. Le NB « no global @RestControllerAdvice » répété dans `CLAUDE.md` est **obsolète** (historique d'avant le 2026-06-14).
- **Scope volontairement étroit** : l'advice ne vise que `controller.api`, **pas** `com.clinic.backend.controller` au sens large — sinon il avalerait les exceptions des contrôleurs `controller.web` (vues Thymeleaf) et les rendrait en JSON. Le seul `@RestController` hors `controller.api` est `AuthController` (`controller`), dont les erreurs sont gérées **inline** (423/401, fait en P1.3).
- **Vérifié** (H2, JWT admin) : surpaiement facture → **400** JSON standard (`Le montant dépasse le reste à payer (6000.00)`) ; facture déjà soldée → **409** JSON (`Cette facture est déjà soldée`) ; **plus aucun 500 brut**.
- **Raffinement P1.4b — fait (2026-06-19)** : les lookups not-found **par id** renvoient désormais **404** (`ResourceNotFoundException`). Décision clé : `ResourceNotFoundException extends IllegalArgumentException` → tous les `catch (IllegalArgumentException)` des WebController restent intacts (sous-classe) tout en obtenant 404 côté API (handler le plus spécifique). Conversion par heuristique dans 15 services : tout `introuvable` → 404, **sauf** les FK de body (`dto.get*Id()` en création/màj) qui restent **400** (validation de requête). Couvert par `ApiErrorMappingTest` (GET facture inexistante → 404, POST avec patient inexistant → 400, surpaiement → 400, facture soldée → 409).
- **Critère d'acceptation** : dispensation insuffisante / surpaiement via API → JSON propre, plus de 500. — ✅ (mapping réel : insuffisant/surpaiement = `IllegalArgumentException`→**400** ; conflits d'état type « déjà dispensée/soldée » →**409**).

### P1.5 — Tests (sécurité + invariants métier)
- [x] Statut : **fait** (2026-06-19) — **26 tests verts** (`mvnd test` BUILD SUCCESS).
- **Réalisé** : `spring-security-test` ajouté au pom ; `src/test/resources/application-test.properties` (profil `test` : H2 dédié + secret JWT de test) ; tests sous `@ActiveProfiles("test")` (DataInitializer seedé, profil `!prod`). 5 classes :
  - `ClinicApplicationTests` — smoke contexte (Flyway migre, beans se câblent, Hibernate valide).
  - `LoginAttemptServiceTest` (5, Mockito pur) — invariants P1.3 : +1 par échec, verrou à 5, reset au succès, verrou expiré repart de 0, utilisateur inconnu/null ne casse rien.
  - `SecurityMatrixTest` (9, MockMvc + `@WithMockUser`) — API sans token refusée, page web sans session → redirect `/login`, `/login` public ; `/admin/audit` (ADMIN 200 / MEDECIN 403), `/reports/dashboard` (MEDECIN 200 / CAISSIER 403), `/reports/financial` (CAISSIER 200 / MEDECIN 403).
  - `PageRenderSmokeTest` (6, MockMvc + `@WithUserDetails("admin")`) — `/dashboard`, `/patients`, `/appointments`, `/billing`, `/reports`, `/admin/audit` → 200 (exerce layout+sidebar = la classe du bug `mod`→`navMod`). Principal réel via `userDetailsServiceBeanName="userDetailsServiceImpl"`.
  - `BusinessInvariantTest` (5, `@SpringBootTest` + `@Transactional` rollback + `@WithUserDetails`) — couverture assurance (80% sur 1000 → ins 800/patient 200), surpaiement rejeté (IllegalArgument), paiement sur facture soldée rejeté (IllegalState), **FIFO** (lot J+30 vidé avant le lot +2 ans : reste 5 sur le lointain), stock insuffisant rejeté.
- **Différé** : **étape 4 (Testcontainers PostgreSQL) non faite** — Docker indisponible en local (même contrainte qu'en P1.1), `mvnd test` doit rester vert ici. À ajouter quand un runtime Docker (CI) sera dispo, pour valider Flyway sur le vrai moteur. Double-admission/transition d'hospitalisation non couvertes (FIFO + couverture + gardes de paiement = invariants les plus à risque, priorisés).
- **Pourquoi** : **0 test** dans `backend/src/test`. Pas de filet pour les régressions (ex. le bug `mod` aurait été attrapé par un test de rendu).
- **Critère d'acceptation** : `mvnd test` vert, matrice de rôles couverte. — ✅ (26 tests, matrice RBAC + rendu + invariants).

---

## 🟠 P2 — Attentes d'un EHR sérieux

### P2.1 — Interopérabilité FHIR R4
- [x] Statut : **fait** (2026-06-20)
- **Réalisé** : dépendance `ca.uhn.hapi.fhir:hapi-fhir-structures-r4:7.4.0` (HAPI 7.x = Jakarta EE, compatible Boot 3 / Java 21). Nouveau pkg `com.clinic.backend.fhir` : `FhirConfig` (bean `FhirContext` R4 partagé, parsers créés à la demande), `FhirMapper` (`@Component` pur — projette les entités vers `org.hl7.fhir.r4.model.*`), `FhirService` (`@Transactional(readOnly=true)` — **mappe dans la tx** pour résoudre les lazy assocs, OSIV off), `FhirController` (`/fhir/**`, sérialise en `application/fhir+json`), `FhirExceptionHandler` (`@RestControllerAdvice` scopé `…fhir` → rend les erreurs en **`OperationOutcome`**, pas en JSON générique). Ressources exposées (lecture) :
  - **Patient** : `GET /fhir/Patient/{id}` + `GET /fhir/Patient?name=` (Bundle searchset). Identifiants record-number/national-id, nom, sexe (M/F/AUTRE→male/female/other), naissance, télécom, adresse.
  - **Encounter** (depuis `Consultation`) : `GET /fhir/Encounter/{id}` + `?patient=`. Statut TERMINE/EN_COURS/ANNULE→finished/in-progress/cancelled, class AMB, sujet/praticien, période, motif (diagnostic ou plainte), serviceType=département.
  - **Observation** (constantes **+** labo) : `GET /fhir/Observation/{id}` + `?patient=`. Id encode la source : `lab-{labResultId}` (catégorie laboratory, valeur Quantity si numérique sinon String, plage de réf, interprétation `A` si anormal) et `vital-{consultId}-{clé}` (catégorie vital-signs, codes LOINC, TA en composants systolique/diastolique).
  - **MedicationRequest** (depuis `PrescriptionItem`, une ligne = un MedicationRequest) : `GET /fhir/MedicationRequest/{id}` + `?patient=`. Statut active/completed (dispensée), medication en texte, posologie (dosage/fréquence/durée) + instructions patient + quantité.
  - `GET /fhir/metadata` → **CapabilityStatement** (public).
- **Sécurité** : `/fhir/**` ajouté au `securityMatcher` de la **chaîne stateless JWT** (même modèle que `/api/**`) ; seul `/fhir/metadata` est `permitAll`. Repos ajoutés : `LabResultRepository.findWithRefsById`, `PrescriptionItemRepository.findWithRefsById`, `PrescriptionRepository.findByPatientWithItems`. **Aucune migration Flyway** (couche de projection pure, comme les rapports du module 14) — reste à v17.
- **Vérifié** : `mvnd test` **56 verts** (was 47 ; +`FhirApiTest` 9 : Patient read FHIR valide + bundle, Encounter→Patient/1, Observation bundle combine `vital-1-*` + `lab-*`, MedicationRequest bundle ≥2 lignes « Oméprazole », metadata public = CapabilityStatement, sans token → 4xx, inexistant → **404 OperationOutcome** `not-found`, paramètre `patient` manquant → **400 OperationOutcome** `required`). Les `@SpringBootTest` valident le boot avec le `FhirContext` câblé.
- **Pourquoi** : standard 2025 (ONC Cures Act, Patient Access API). Sépare un « logiciel interne » d'un EHR.
- **Étapes** : exposer `Patient`, `Encounter`, `Observation` (constantes/labo), `MedicationRequest` (ordonnances) en FHIR via HAPI FHIR ; endpoints `/fhir/**`.
- **Critère** : `GET /fhir/Patient/{id}` renvoie une ressource FHIR valide. — ✅
- **Reste (optionnel)** : écritures FHIR (`POST`/transaction Bundle), serveur HAPI complet (`RestfulServer` + `_format`/recherche avancée/pagination), ressources `Practitioner`/`DiagnosticReport`/`Condition`/`AllergyIntolerance`, codage LOINC réel des analyses (mapping `lab_test_catalog.code`→LOINC) et SMART-on-FHIR.

### P2.2 — Diagnostics CIM-10 codés (fin du texte libre)
- [x] Statut : **fait** (2026-06-19)
- **Réalisé** : table de référence `icd10_catalog` (`V16`, ~50 codes amorcés — pathologies fréquentes en clinique africaine : paludisme, drépanocytose, HTA, diabète, IRA, gastrite, CPN…). Pkg `catalog` : `Icd10Code` (code unique/title/category/active), `Icd10CodeRepository` (`search` JPQL actif-only, correspondance code **ou** libellé, préfixe-code priorisé, borné par `Pageable`), `Icd10Service` (search bornée à 20, CRUD + toggle), `Icd10CodeDto`. REST `Icd10ApiController` (`/api/icd10` : `GET /search?q=` ouvert aux authentifiés, écritures ADMIN). Admin CRUD `Icd10WebController` (`/admin/icd10` list+form) + nav `ADMIN_ICD10` (🏷️, auto-visible ADMIN). **Auto-complétion câblée dans le formulaire de consultation** : champ de recherche + dropdown JS qui appelle `GET /consultations/icd10/search` (endpoint servi sous la **chaîne web/session** — `/api/**` est JWT stateless, inaccessible depuis la page ; gardé `ADMIN/MEDECIN/INFIRMIER`), clic = ajout du code (dédup) dans `icd10Codes` (séparés par virgule). Le `diagnosis` texte libre **reste obligatoire** pour clôturer (inchangé). CSS `.icd10-suggestions` ajouté à `app.css`. `consultations.icd10_codes` (déjà en V6) inchangé — alimenté désormais par le catalogue. `docs/DATABASE.md` §5 documente `icd10_catalog`.
- **Vérifié** : `mvnd test` **41 verts** (was 37 ; +`Icd10CatalogTest` : recherche par code `J06`→`J06.9`, par libellé `diab`→`E11`, non-admin POST→403, doublon `I10`→400). Les `@SpringBootTest` valident Flyway→v16 + Hibernate validate sur la nouvelle table.
- **Pourquoi** : diagnostic actuellement en texte libre → épidémiologie et facturation par acte fragiles.
- **Étapes** : table de référence `icd10_catalog` + auto-complétion dans la consultation ; garder le texte libre en complément.
- **Reste (optionnel)** : brancher le « top pathologies » des rapports (module 14) sur `icd10_codes` plutôt que sur `diagnosis` texte libre ; étendre l'amorce (catalogue complet importable) ; afficher le libellé des codes (pas que le code) sur la fiche consultation/détail.

### P2.3 — Export PDF / Excel binaire
- [x] Statut : **fait côté web** (2026-06-19) ; reste optionnel : `format=pdf|excel` côté API REST.
- **Réalisé (socle)** : libs au pom — **openhtmltopdf 1.0.10** (LGPL, choisi plutôt qu'iText/AGPL pour le SaaS) + **jsoup** (HTML→XHTML) + **Apache POI 5.2.5** (xlsx). Nouveau pkg `export` : `PdfExportService` (rend un template Thymeleaf hors requête web avec `pdf=true` → jsoup → openhtmltopdf → octets ; **réutilise les vues d'impression existantes**, pas de duplication) et `ExcelExportService` (POI, largeurs fixes headless-safe).
- **Documents PDF livrés** : reçu de facture (`/billing/invoices/{id}/receipt/pdf`, **validé en réel**), ordonnance (`/prescriptions/{id}/pdf`, **imprimée OK par l'utilisateur**), bulletin labo (`/lab/requests/{id}/bulletin/pdf`), compte-rendu imagerie (`/radiology/requests/{id}/bulletin/pdf` — galerie d'images masquée en PDF via `th:if="${!pdf}"`, à embarquer en base64 plus tard), et **3 rapports** via un template print générique `reports/pdf-report.html` : financier, activité, épidémiologie (`/reports/{financial,activity,epidemiology}/pdf?month&year`). **Excel** : impayés (`/reports/outstanding/excel`). Boutons « ⬇ PDF » / « ⬇ Excel » sur toutes les pages concernées (toolbar/actions masquées en PDF ou via `.no-print`).
- **Tests** : `ExportTest` (7) — reçu + 2 bulletins + 3 rapports = `%PDF-` valides ; impayés = xlsx (`PK`). **37 tests verts.**
- **Reste (optionnel)** : `format=pdf|excel` sur `/api/reports/*` (côté API REST, pour le client JavaFX), embarquer les images radio en base64 dans le PDF, et export Excel d'autres rapports (caisse du jour). NB : fidélité visuelle confirmée sur l'ordonnance ; les autres reposent sur la même chaîne.
- **Pourquoi** : ordonnances, bulletins labo/imagerie, reçus, rapports. Aujourd'hui HTML print-only.
- **Étapes** : ~~iText/JasperReports~~ openhtmltopdf + Apache POI au pom ; activer `format=pdf|excel` sur `/api/reports/*` et boutons « PDF » sur les bulletins/reçus.

### P2.4 — Portail patient
- [x] Statut : **fait** (2026-06-19)
- **Réalisé** : lien compte↔dossier via `patients.user_id` (`V17`, FK + UNIQUE vers `users`) — `Patient.portalUser` (`@OneToOne` LAZY), `PatientRepository.findByPortalUserIdAndDeletedAtIsNull`. Rôle `PATIENT` enfin câblé : entrée `RoleProfile.PATIENT` (homepage `/portal`, **0 module** staff, 0 type de notif) → fixe le redirect post-login **et** retire `PATIENT` du fallback `fromRole`→`MEDECIN` qui l'aurait baladé dans la sidebar staff. Nouveau pkg `com.clinic.backend.portal` : `PortalService.currentPatient()` (résout le dossier du `User` courant via `SecurityContextHolder` ; lève `ResourceNotFoundException` si non rattaché → tout passe par ce point, **un patient ne voit que son dossier**) + `PortalWebController` (`@PreAuthorize("hasRole('PATIENT')")`) : `/portal` (accueil + 3 prochains RDV), `/portal/appointments` (historique), `/portal/appointments/request` (GET/POST — **patientId forcé serveur**, statut `PLANIFIE` = demande à confirmer par l'accueil, réutilise `AppointmentService.create` donc gardes conflit/passé), `/portal/record` (lecture seule : consultations + analyses/imagerie **validées uniquement** + factures). Mise en page dédiée `portal/layout.html` (top-nav, **aucune sidebar staff**) + 5 vues (`home/appointments/appointment-form/record/no-record`) ; bloc CSS `.portal-*` ajouté à `app.css`. `@ExceptionHandler(ResourceNotFoundException)` → page `no-record` propre si compte non rattaché. Seed : compte `patient/patient123` (rôle PATIENT) rattaché à p1 (Aminata Diallo) → portail peuplé (RDV, consultations, labo validé anormal, facture PARTIEL).
- **Vérifié** : `mvnd test` **47 verts** (was 41 ; +`PortalTest` 5 : accueil/RDV/dossier/formulaire rendent 200 sous `@WithUserDetails("patient")`, demande sans médecin → re-render 200 ; +1 `SecurityMatrixTest` : MEDECIN → **403** sur `/portal`). Les `@SpringBootTest` valident Flyway→v17 + Hibernate validate sur la colonne `patients.user_id`.
- **Pourquoi** : accès patient à ses résultats/RDV — attendu réglementaire.
- **Étapes** : espace `/portal/**` gated `PATIENT` : voir son dossier (lecture), ses RDV, demander un RDV.
- **Reste (optionnel)** : annulation d'un RDV par le patient, téléchargement PDF de ses bulletins/ordonnances depuis le portail, self-service de rattachement (l'admin crée et lie le compte aujourd'hui), profil/changement de mot de passe.

### P2.5 — Chiffrement (transit + repos)
- [x] Statut : **fait** (2026-06-20)
- **Réalisé** : les 3 facettes, doc transverse `docs/SECURITY-ENCRYPTION.md`.
  - **Repos / colonnes PHI (cœur codé+testé)** : nouveau pkg `com.clinic.backend.crypto` — `AesGcmCipher` (AES-256-GCM, clé dérivée SHA-256 du secret, IV aléatoire 12o/écriture, tag 128 bits, format `gcm:<base64(IV‖ct+tag)>`, **tolère les valeurs en clair legacy** au déchiffrement), `PhiStringConverter` (`@Converter` JPA non-autoApply, cipher en réf. statique injectée par `CryptoConfig` → Hibernate instancie le converter hors Spring), `CryptoConfig` (bean `AesGcmCipher` depuis `app.encryption.key`, fail-fast prod). Posé via `@Convert` sur 5 colonnes `TEXT` de `patients` : `address`, `allergies`, `chronic_conditions`, `medical_history`, `notes`. **Pas chiffrées** : `first_name`/`last_name`/`record_number`/`phone`/`national_id` (recherche `LIKE` + unicité — les chiffrer casserait la recherche patient). **Aucune migration Flyway** (colonnes déjà `TEXT`, reste à v17). Clé : `application-{dev,test}.properties` (fixe) ; prod `app.encryption.key=${APP_ENCRYPTION_KEY}` (pas de défaut → fail-fast comme JWT).
  - **Transit** : service `nginx` (reverse-proxy TLS) dans `docker-compose.yml` — termine HTTPS, relaie vers `backend:8080`, **backend non exposé sur l'hôte** (`expose` au lieu de `ports`), HTTP→HTTPS 301, HSTS 1 an, TLSv1.2/1.3. `nginx/nginx.conf` + `nginx/README.md` (génération certif auto-signé LAN via openssl / Let's Encrypt). Spring prod : `server.forward-headers-strategy=framework` + cookie session `Secure`/`HttpOnly`. Certs gitignorés (`nginx/certs/`).
  - **Volumes** : documenté (exploitation, non codable) — LUKS/dm-crypt sur `postgres_data` + `backend_uploads`, dumps chiffrés. `.env.example` : `APP_ENCRYPTION_KEY` ajouté (avec avertissement rotation).
- **Vérifié** : `mvnd test` **63 verts** (was 56 ; +`EncryptionConverterTest` 7) — unitaires `AesGcmCipher` (aller-retour, null, IV aléatoire/écriture, legacy en clair tel quel, **altération détectée**, mauvaise clé rejetée) + **intégration JPA** : patient sauvé → lecture brute JDBC de `medical_history` = `gcm:…` sans le clair, relecture via entité = texte clair (le converter déchiffre). TLS/volumes non testables localement (Docker absent, même contrainte que P1.1).
- **Étapes** : HTTPS aussi en LAN (reverse-proxy + certif interne) ; volume DB chiffré ou colonnes PHI sensibles chiffrées ; uploads sur volume chiffré.
- **Reste (optionnel)** : étendre le chiffrement aux constantes/diagnostics consultation + résultats labo (nécessite d'élargir des VARCHAR courts en TEXT) ; chiffrer les fichiers uploadés au repos applicatif (au-delà du volume) ; rotation de clé outillée (re-chiffrement) ; certif Let's Encrypt automatisé (certbot/Caddy).

---

## 🟡 P3 — Différenciation & contexte africain

- [x] **P3.1 — Mode hors-ligne / PWA** — **fait (slice fondation, 2026-06-20)**. App installable + résiliente aux coupures, **sans jamais mettre en cache de PHI/auth** (choix EHR délibéré). Ressources statiques (aucune migration Flyway) : `manifest.webmanifest` (display standalone, theme `#0f172a`, icône SVG `images/icon.svg` purpose `any maskable`), `sw.js` (service worker scope `/`), `offline.html` (repli auto-suffisant inline), `js/pwa.js` (enregistrement, dégradation gracieuse). **Stratégie SW (stricte)** : GET même-origine uniquement ; app shell (`/css/`,`/js/`,`/images/`,manifest) en *stale-while-revalidate* ; navigations en *réseau-d'abord* → `/offline.html` hors-ligne (**pages HTML jamais cachées** = pas de PHI périmé) ; `/api`,`/fhir`,`/uploads`,`/h2-console`,`/login`,`/logout` **bypass total** (réseau strict). `SecurityConfig` : `permitAll` sur `/manifest.webmanifest`,`/sw.js`,`/offline.html` + CSP `manifest-src`/`worker-src 'self'`. Liens `<head>` (manifest/theme-color/apple-touch-icon/pwa.js) dans `base.html` + `portal/layout.html` + `login.html` (installable avant connexion). **Vérifié** : `mvnd test` **66 verts** (+3 `SecurityMatrixTest` : manifest/sw.js/offline.html publics 200). **Reste (option lourde)** : file de synchro des **écritures hors-ligne** (queue IndexedDB + background sync + rejouabilité serveur), précache de pages-clés, icônes PNG (actuellement SVG seul), invite d'install personnalisée (`beforeinstallprompt`).
- [x] **P3.2 — i18n FR/EN/AR** — **fait (slice fondation, 2026-06-20)**. Socle complet + bascule live, sur le chrome partagé (sidebar/topbar), la connexion et le tableau de bord ; **arabe = RTL**. 3 bundles UTF-8 `messages{,_en,_ar}.properties` (app/nav/login/dashboard/common). `WebConfig` : `CookieLocaleResolver("clinicLang")` (défaut FR, cookie 1 an, survit logout/redémarrage) + `LocaleChangeInterceptor("lang", ignoreInvalidLocale)` → bascule via `?lang=fr|en|ar` sur **n'importe quelle page, y compris `/login`**. Enum `Module` gagne un `labelKey` (`nav.*`) ; `base.html` rend la sidebar via `#{${navMod.labelKey}}` (+ sections via `#{nav.section.*}`), `<html th:lang th:dir>` (rtl si `ar`), sélecteur de langue FR/EN/AR dans la topbar (lien `@{${currentUri}(lang=…)}`, conserve la page). `login.html` + `dashboard.html` entièrement traduits (titre layout via `#{dashboard.title}`). `app.css` : `.lang-switch` + bloc `[dir="rtl"]` (sidebar→droite, `.main` margin-right, alignements). `spring.messages.basename/encoding=UTF-8/fallback-to-system-locale=false`. **Vérifié** : `mvnd test` **70 verts** (+4 `I18nTest` : login défaut FR « Se connecter », `?lang=en` « Sign in », `?lang=ar` `dir="rtl"`+arabe, sidebar EN authentifiée). **Reste (gros du volume) :** traduire les ~75 autres templates (toujours FR en dur) + les messages flash des contrôleurs web + les libellés d'enum (statuts, méthodes de paiement…) ; clés `common.*` déjà posées comme socle réutilisable. AR partiel sur RTL fin (formulaires/tables non audités).
- [x] **P3.3 — Webhook Mobile Money réel** (Orange/Wave/MTN) — **fait (slice récepteur, 2026-06-21)**. Endpoint `POST /api/payments/webhook/{provider}` (orange|wave|mtn) qui **applique automatiquement l'encaissement** sur la facture, à la place de la saisie manuelle. **Authentification** par signature HMAC-SHA256 du corps brut (`X-Webhook-Signature`, schéma Stripe-like) — `WebhookSignatureVerifier` (comparaison à temps constant), secret `app.webhook.secret` (dev/test fixe, prod `${MOBILE_MONEY_WEBHOOK_SECRET}` fail-fast) ; chemin `permitAll` sur la chaîne API stateless (pas de JWT). **Idempotence + audit** : table `V18 payment_webhook_events` (UNIQUE `(provider, transaction_id)`), `PaymentWebhookEvent`/repo ; un rejeu → `DUPLICATE` sans double paiement. `PaymentWebhookService` (volontairement **non-`@Transactional`** au niveau classe : journalise chaque issue même si `recordPayment` annule sa propre tx) : vérifie signature → mappe provider→mode (ORANGE_MONEY/WAVE/MTN_MOMO) → parse → idempotence → `BillingService.recordPayment` (réutilise toutes les gardes : annulée/soldée/surpaiement) ; `InvoiceRepository.findByInvoiceNumber`. **Sémantique HTTP** : 200 acquitté (PROCESSED/DUPLICATE/REJECTED métier → pas de rejeu), 401 signature, 400 provider inconnu/JSON illisible. `.env.example` + 3 profils mis à jour. **Vérifié** : `mvnd test` **85 verts** (+5 `PaymentWebhookTest` : paiement valide → PAYE, signature invalide → 401, provider inconnu → 400, facture introuvable → REJECTED/200, **rejeu idempotent** → DUPLICATE sans double encaissement). **Reste (optionnel) :** initiation du paiement (push USSD/STK), vue admin du journal `/admin/...` des webhooks, intégration SDK réelle par agrégateur (schémas de signature propres à chacun), réconciliation/relance, montants partiels multiples.
- [x] **P3.4 — Accessibilité WCAG 2.2** — **fait (slice chrome + design system, 2026-06-21)**. Passe transverse via le layout partagé + `app.css` (tout l'app en bénéficie). **Clavier / focus (2.4.7, 2.4.11)** : anneau `:focus-visible` net et cohérent (2px, offset) sur tous les contrôles — blanc sur le sidebar sombre, bleu sur surfaces claires, `Highlight` en `forced-colors` ; `.form-control:focus-visible` ajouté pour battre l'ancien `outline:none` (spécificité). **Évitement de bloc (2.4.1)** : `.skip-link` (1er focusable → `#main-content`), `<main id="main-content" tabindex="-1">`. **ARIA / repères (1.3.1, 4.1.2)** : `<nav aria-label>` (sidebar + sélecteurs de langue), `aria-current="page"` sur l'item actif, `aria-current="true"` sur la langue active, `aria-hidden="true"` sur les icônes/emojis décoratifs, `role="alert"`/`role="status"` sur les alertes de connexion. **Contraste (1.4.3)** : `--text-400` #94a3b8→#6b7280 (2.8→4.9:1) et `--sidebar-section` #475569→#7e8ba3 (2.3→~5:1). **Taille de cible (2.5.8)** : sélecteurs de langue ≥24×24. **Mouvement réduit** : `@media (prefers-reduced-motion)`. 3 clés i18n `a11y.*` (FR/EN/AR). **Vérifié** : `mvnd test` **73 verts** (+3 `A11yTest` : skip-link+landmark+nav nommée, `aria-current` actif, `role="alert"` connexion). **Reste :** audit fin des ~75 templates (labels de champs orphelins, boutons icône-seule sans `aria-label`, `<th scope>` sur les tables, ordre de tabulation des formulaires complexes, contraste des badges colorés), tests automatisés axe-core, audit lecteur d'écran réel.
- [x] **P3.5 — Recherche globale + raccourcis clavier** — **fait (slice palette de commandes, 2026-06-21)**. Palette type ⌘K dans le chrome partagé, agrégeant plusieurs sources **avec filtrage par rôle** (mêmes règles que la sidebar via `RoleProfile`). Backend : pkg `search` — `GlobalSearchService` (`@Transactional(readOnly=true)`, mapping en-tx OSIV off) renvoie une liste unique de `SearchResultDto` (type/catégorie i18n/label/sublabel/url/icône) sur 3 sources : **Navigation** (modules du rôle dont le libellé traduit matche, 0 DB, dès 1 car.), **Patients** (si module PATIENTS, ≥2 car.), **Factures** (si module BILLING — `InvoiceRepository.searchByNumber`). `GlobalSearchWebController` (chaîne web/session comme l'autocomplete CIM-10) : `GET /search/suggest?q=` → JSON, `GET /search?q=` → page de repli `search/results.html`. Front : `js/search.js` (vanilla, debounce 160 ms + anti-course `seq`, ouverture clic / **Ctrl·⌘+K** / **«/»** hors-saisie, nav clavier ↑↓ Entrée Échap, ARIA combobox/listbox + `aria-activedescendant`, fond cliquable, restaure le focus) ; markup palette + déclencheur topbar (`🔍 Rechercher Ctrl K`) dans `base.html` ; styles `.cmdk-*`/`.topbar-search` dans `app.css` (responsive, RTL). 12 clés i18n `search.*` (FR/EN/AR). **Vérifié** : `mvnd test` **77 verts** (+4 `GlobalSearchTest` : admin trouve patient + module nav, **LABORANTIN ne voit pas les patients** (gating prouvé), page de résultats 200). **Reste (optionnel) :** étendre aux consultations / RDV / médicaments, surlignage du terme, récents/favoris, recherche serveur paginée complète, scoping multi-tenant.
- [x] **P3.6 — Vue patient « coup d'œil » + timeline** — **fait (2026-06-21)**. Nouvel onglet **Aperçu** (actif par défaut) en tête du dossier `patients/detail.html`, **au-dessus** des onglets existants (inchangés). Backend : `PatientOverviewService` (pkg `patient`, **pur/sans DB** — opère sur les données déjà chargées par le contrôleur, zéro requête de plus, aucun accès lazy → sûr OSIV off) construit un `PatientOverviewDto` { âge, groupe sanguin, allergies, **dernières constantes** (consultation la plus récente avec mesures → TA/T°/pouls/SpO₂/poids), **alertes** `OverviewAlertDto` (RED→ORANGE→INFO : allergie, grossesse à risque, **hospitalisé actuellement**, tension >140/90, labo anormal, **impayés** avec reste dû, antécédents), **timeline** `TimelineEventDto` unifiant consultations/labos/imageries/séjours/factures, triée desc, deep-link par évènement }. `PatientWebController.detail` capture les listes une fois et les passe au builder (pas de double appel). Front : section Aperçu (chips d'alerte, cartes synthèse, ligne constantes, `<ul class="timeline">`) + styles `.overview-*`/`.ov-*`/`.vitals-*`/`.timeline`/`.tl-*` dans `app.css` (RTL ok). **Vérifié** : `mvnd test` **80 verts** (+2 `PatientOverviewServiceTest` unitaire : priorité RED en tête, 6 alertes dérivées, timeline 4 évts triée + patient vierge = 0 ; +1 render `/patients/1` contient « Chronologie du dossier »). **Reste (optionnel) :** i18n de l'onglet (FR en dur, cohérent avec le reste de detail.html), filtres/onglets de timeline par type, pagination si dossier volumineux, intégrer les CPN maternité comme évènements, dernières constantes sparkline.
- [ ] **P3.7 — Télémédecine légère** : lien de téléconsultation (sans IA), tendance forte et faisable.

---

## ⚪ P4 — Haut de gamme / plus tard

- [ ] **P4.1 — Scribe IA ambiant** : transcription consultation → note structurée (avec un modèle Claude). *La* tendance phare 2026 — après les bloquants.
- [ ] **P4.2 — Multi-tenant** : une instance = plusieurs cliniques (déjà roadmap Phase 5).
- [ ] **P4.3 — Spring Actuator + monitoring** (Prometheus/Grafana, UptimeRobot) — esquissé au module 15.
- [ ] **P4.4 — Refresh tokens + révocation JWT** : aujourd'hui token 24h non révocable.

---

## Journal de progression (à remplir à chaque session)

| Date | Item | Résultat |
|---|---|---|
| 2026-06-19 | (création) | Backlog créé suite à l'analyse comparative + audit code. Bug RBAC `mod`→`navMod` corrigé (commit 4ed3535). |
| 2026-06-19 | **P1.1** | Profils dev/prod + secrets externalisés (fail-fast), seed démo gated `!prod`, bootstrap admin prod via env, compose+`.env.example`. Vérifié dev/prod/fail-fast. Boot Postgres réel à confirmer au déploiement (Docker indispo en local). |
| 2026-06-19 | **P1.2** | Journal d'audit : pkg `audit` (`@Audited` + AOP `@AfterReturning`, écriture `REQUIRES_NEW`), V14, vue admin `/admin/audit` (ADMIN). 14 méthodes auditées. Vérifié : 3 actions → 3 traces (auteur/entité/id/IP), filtres OK, non-admin 403. |
| 2026-06-19 | **P1.3** | Anti-brute-force : V15 (`failed_attempts`/`locked_until`), `LoginAttemptService` (5 échecs → verrou 15 min, reset au succès) branché par événements Spring Security (web+API), `isAccountNonLocked()` câblé, `LoginFailureHandler`→`?locked=true`, API→423/401 JSON. Vérifié : API 5×401→423, autre compte 200, reset prouvé (8 échecs entrecoupés de succès → jamais verrouillé), web 302→`/login?locked=true`. Rate-limit IP (Bucket4j) non fait. |
| 2026-06-19 | **P1.4** | **Déjà fait** (commit `e2db645`, 2026-06-14, avant ce backlog). `GlobalExceptionHandler` = `@RestControllerAdvice` scopé `controller.api` (couvre les 16 contrôleurs API) : IllegalArgument→400, IllegalState→409, ResourceNotFound→404, Auth→401, AccessDenied→403, fallback→500, format standard. Vérifié : surpaiement→400 JSON, facture soldée→409 JSON, plus de 500 brut. NB CLAUDE.md « no @RestControllerAdvice » obsolète. Raffinement reporté : not-found→400 (services lèvent IllegalArgument au lieu de ResourceNotFound) — à faire en « P1.4b » après P1.5. |
| 2026-06-19 | **P1.5** | Tests : **0 → 26 verts** (`mvnd test` OK). `spring-security-test` + profil `test` (H2). 5 classes : smoke contexte, `LoginAttemptServiceTest` (P1.3), `SecurityMatrixTest` (matrice RBAC MockMvc), `PageRenderSmokeTest` (anti-régression templating), `BusinessInvariantTest` (couverture assurance, surpaiement, facture soldée, FIFO, stock insuffisant). Testcontainers PostgreSQL différé (Docker indispo local). |
| 2026-06-19 | **P1.4b** | not-found par id → **404** (`ResourceNotFoundException extends IllegalArgumentException` : zéro régression sur les `catch` web, 404 côté API). Conversion heuristique dans 15 services (FK de body `dto.get*` restent 400). +1 classe `ApiErrorMappingTest` (404/400/400/409). **30 tests verts**. |
| 2026-06-19 | **P2.3** (slice) | Export : openhtmltopdf+jsoup+POI au pom, pkg `export` (`PdfExportService` réutilise les vues print via `pdf=true`, `ExcelExportService`). Livré : reçu PDF, ordonnance PDF, impayés Excel + boutons. `ExportTest` (PDF/xlsx valides). **32 tests verts**. Reste : bulletins labo/radio PDF, rapports PDF, `format=pdf\|excel` sur `/api/reports/*`. |
| 2026-06-19 | **P2.3** (fin web) | Complété : bulletins **labo** + **imagerie** PDF, **3 rapports** PDF (financier/activité/épidémio) via template print générique `reports/pdf-report.html`. Boutons partout. `ExportTest`→7. **37 tests verts**. Ordonnance imprimée en réel (fidélité OK). Reste optionnel : `format=pdf\|excel` API REST + images radio en base64. |
| 2026-06-19 | **P2.4** | Portail patient : `V17` `patients.user_id` (FK+UNIQUE), `Patient.portalUser`, `RoleProfile.PATIENT` (homepage `/portal`, 0 module). Pkg `portal` : `PortalService.currentPatient()` (un patient ne voit que son dossier), `PortalWebController` gated PATIENT (`/portal`, `/appointments`(+request, patientId forcé, statut PLANIFIE), `/record` lecture seule résultats validés). Layout dédié `portal/layout` (pas de sidebar staff) + 5 vues + CSS `.portal-*`. Seed `patient/patient123` ↔ p1. `PortalTest`(+5) + RBAC MEDECIN→403(+1). **47 tests verts.** |
| 2026-06-20 | **P2.1** | Interopérabilité FHIR R4 : dép. `hapi-fhir-structures-r4:7.4.0`, pkg `fhir` (`FhirConfig`/`FhirMapper`/`FhirService` mappe-en-tx/`FhirController`/`FhirExceptionHandler`→`OperationOutcome`). Lecture `Patient`, `Encounter`(consultation), `Observation`(constantes `vital-*` + labo `lab-*`), `MedicationRequest`(lignes d'ordonnance) + `/fhir/metadata` (CapabilityStatement). `/fhir/**` sur la chaîne JWT stateless (metadata public). Pas de migration (projection pure, reste v17). `FhirApiTest` (+9). **56 tests verts**. Reste optionnel : écritures FHIR, serveur HAPI complet, ressources additionnelles, codage LOINC réel. |
| 2026-06-20 | **P2.5** | Chiffrement transit+repos : pkg `crypto` (`AesGcmCipher` AES-256-GCM clé SHA-256/fail-fast prod, `PhiStringConverter` JPA, `CryptoConfig`) chiffre 5 colonnes PHI `patients` (address/allergies/chronic_conditions/medical_history/notes ; pas les colonnes recherchées). nginx reverse-proxy TLS dans compose (backend non exposé, HTTP→HTTPS, HSTS) + `nginx/{nginx.conf,README}`, Spring prod forward-headers + cookie Secure. `docs/SECURITY-ENCRYPTION.md` (+volumes LUKS exploitation). `.env.example` `APP_ENCRYPTION_KEY`. Aucune migration (colonnes déjà TEXT). `EncryptionConverterTest`(+7, dont intégration colonne chiffrée en base). **63 tests verts**. |
| 2026-06-20 | **P3.1** | Mode hors-ligne / PWA (fondation) : `manifest.webmanifest`+`sw.js`+`offline.html`+`js/pwa.js`+icône SVG ; SW **réseau-d'abord** pour les pages (→`offline.html`) et **stale-while-revalidate** pour l'app shell, **zéro cache PHI/auth** (`/api`,`/fhir`,`/uploads`,`/login`,`/logout` bypass). `SecurityConfig` permitAll + CSP `manifest-src`/`worker-src`. `<head>` PWA dans base/portal/login. Aucune migration. `mvnd test` **66 verts** (+3). Reste : file de synchro écritures hors-ligne (IndexedDB+background sync). |
| 2026-06-19 | **P2.2** | Diagnostics CIM-10 codés : `V16` `icd10_catalog` (~50 codes amorcés), pkg `catalog` (`Icd10Code`/repo `search`/service/DTO), `/api/icd10` (search ouvert, écritures ADMIN), admin `/admin/icd10` + nav `ADMIN_ICD10`. Auto-complétion JS dans le formulaire consultation via `/consultations/icd10/search` (servi en session, `/api/**` étant JWT) ; texte libre `diagnosis` conservé + obligatoire. DATABASE.md §5 à jour. `Icd10CatalogTest` (+4). **41 tests verts**. Reste optionnel : rapports « top pathologies » sur codes, libellés sur la fiche, catalogue complet. |
| 2026-06-20 | **P3.2** | i18n FR/EN/AR (fondation) : 3 bundles UTF-8 `messages{,_en,_ar}.properties`, `CookieLocaleResolver("clinicLang")` défaut FR + `LocaleChangeInterceptor("lang")` → bascule `?lang=` sur toute page (dont `/login`). `Module.labelKey` → sidebar via `#{${navMod.labelKey}}` ; `base.html` `<html th:lang th:dir>` (**RTL si ar**) + sélecteur FR/EN/AR topbar ; `login.html`+`dashboard.html` traduits ; `app.css` `.lang-switch`+`[dir="rtl"]`. `spring.messages.encoding=UTF-8`. `I18nTest` (+4 : FR/EN/AR+RTL/sidebar EN). **70 verts**. Reste (le gros) : ~75 templates + flash contrôleurs + libellés d'enum encore FR en dur (clés `common.*` déjà posées). |
| 2026-06-21 | **P3.4** | Accessibilité WCAG 2.2 (chrome + design system) : `app.css` — `:focus-visible` cohérent (blanc/sidebar, bleu/clair, `forced-colors`), `.skip-link`, `.sr-only`, cibles langue ≥24px, `prefers-reduced-motion`, contrastes `--text-400`/`--sidebar-section` corrigés (≥4.5:1). `base.html` — skip-link, `<main id=main-content tabindex=-1>`, `<nav aria-label>`, `aria-current` (nav+langue), `aria-hidden` icônes. `login.html` — `role=alert/status`, langue `aria-current`. 3 clés `a11y.*` ×3 langues. `A11yTest` (+3). **73 verts**. Reste : audit fin par template (labels, boutons icône-seule, `th scope`, badges), axe-core, lecteur d'écran réel. |
| 2026-06-21 | **P3.5** | Recherche globale (palette ⌘K) : pkg `search` — `GlobalSearchService` agrège Navigation/Patients/Factures en `SearchResultDto`, **filtré par rôle** (`RoleProfile`, comme la sidebar) ; `InvoiceRepository.searchByNumber`. `GlobalSearchWebController` (session) : `/search/suggest` JSON + `/search` page repli. `js/search.js` (Ctrl/⌘+K, «/», nav clavier, ARIA combobox, debounce+anti-course) + déclencheur topbar + palette dans `base.html` ; `.cmdk-*`/`.topbar-search` dans `app.css`. 12 clés `search.*` ×3 langues. `GlobalSearchTest` (+4 : patient, nav, **gating LABORANTIN**, page 200). **77 verts**. Reste : étendre (consultations/RDV/médicaments), surlignage, récents. |
| 2026-06-21 | **P3.6** | Coup d'œil patient + timeline : onglet **Aperçu** (défaut) en tête de `patients/detail.html`. `PatientOverviewService` (pkg `patient`, **pur, 0 DB** sur les données déjà chargées) → `PatientOverviewDto` { âge, groupe, allergies, dernières constantes, **alertes** RED→ORANGE→INFO (allergie/grossesse/hospitalisé/tension/labo anormal/impayés/antécédents), **timeline** unifiée consult/labo/imagerie/séjour/facture triée desc }. `PatientWebController.detail` réutilise les listes (pas de double appel). DTOs `TimelineEventDto`/`OverviewAlertDto`/`PatientOverviewDto`. Styles `.overview-*`/`.timeline`/`.tl-*`. `PatientOverviewServiceTest` (+2) + render `/patients/1` (+1). **80 verts**. Reste : i18n onglet, filtres timeline, CPN comme évènements. |
| 2026-06-21 | **P3.3** | Webhook Mobile Money : `POST /api/payments/webhook/{orange\|wave\|mtn}` encaisse auto la facture. HMAC-SHA256 corps brut (`WebhookSignatureVerifier`, `X-Webhook-Signature`), secret `app.webhook.secret` (dev/test fixe, prod env fail-fast), `permitAll` chaîne API. `V18 payment_webhook_events` UNIQUE `(provider,transaction_id)` → idempotence + audit ; `PaymentWebhookEvent`/repo. `PaymentWebhookService` (non-tx classe : journalise même si `recordPayment` rollback) → mappe provider→mode, parse, idempotence, `recordPayment` (gardes réutilisées) ; `InvoiceRepository.findByInvoiceNumber`. HTTP : 200 acquitté (PROCESSED/DUPLICATE/REJECTED), 401 signature, 400 provider/JSON. `.env.example`+3 profils. `PaymentWebhookTest` (+5 dont idempotence). **85 verts**. Reste : initiation paiement, vue admin journal, SDK réels par agrégateur. |

---

## Sources de l'analyse (2026-06-19)

- Marché EHR : [Software Finder — Top EHR Vendors 2026](https://softwarefinder.com/resources/largest-ehr-vendors) · [Commure — Best EMR 2026](https://www.commure.com/blog-scribe/best-emr-software) · [CapMinds — OpenMRS](https://www.capminds.com/blog/openmrs-the-open-source-emr-solution-for-your-small-practice/) · [Best in KLAS 2026](https://www.healthcareitnews.com/news/best-klas-2026-sees-positive-disruption-tangible-tech-improvements)
- Sécurité : [IT's ASAP — HIPAA 2025](https://www.itsasap.com/blog/hipaa-security-best-practices) · [Accountable — App security PHI](https://www.accountablehq.com/post/healthcare-application-security-hipaa-compliant-best-practices-to-protect-phi) · [OWASP10 — HIPAA & cybersécurité](https://owasp10.com/hipaa-compliance-and-cybersecurity-best-practices-medical-systems-computer-security/)
- UI/UX : [Excellent WebWorld — Trends 2026](https://www.excellentwebworld.com/healthcare-ux-ui-design-trends/) · [UX Studio — 10 trends 2026](https://www.uxstudioteam.com/ux-blog/healthcare-ux)
- Interopérabilité : [Momentum — FHIR/HL7](https://www.themomentum.ai/blog/fhir-hl7-the-foundation-of-healthtech-interoperability) · [PureLogics — Standards 2025](https://purelogics.com/top-interoperability-standards/)
