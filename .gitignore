# Maven
target/
*.jar
*.war
*.class

# Packaging desktop (jpackage) — app-image/installeurs volumineux, artefacts de build
dist/

# NetBeans
nbproject/private/
nbactions.xml
nb-configuration.xml
.nb-gradle/

# IntelliJ / VSCode
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Variables d'environnement sensibles (prod)
# NB : application-prod.properties est versionné — il ne contient AUCUN secret
# (tout via ${ENV}). Les vrais secrets vivent uniquement dans .env (ci-dessous).
.env

# Fichiers uploadés à l'exécution (photos patients, documents — données médicales)
uploads/
backend/uploads/

# Sauvegardes de base de données — JAMAIS versionnées (données médicales)
backups/

# Certificats TLS du reverse-proxy (P2.5) — JAMAIS versionnés (clé privée)
nginx/certs/

#autres
AIDE_MEMOIRE.txt
